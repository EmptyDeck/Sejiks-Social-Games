just like nextRound, I want if the game is 4th game, the gameover button highlights.
also make it not go to the next game 

example:
// 다음 라운드
function nextRound() {
    console.log('다음 라운드 요청');
    
    if (currentRound === 4) {
    // 다음 게임 버튼 강조 효과 추가
    const nextGameBtn = document.getElementById('nextGameBtn');
    nextGameBtn.classList.add('highlight-button');
    return; // 라운드 증가만 하고 리턴
    }
    else{
    openModal('nextRoundModal');
    }

}

To modfiy:
function nextGame() {
    console.log('다음 게임 요청');
    openModal('nextGameModal');
}